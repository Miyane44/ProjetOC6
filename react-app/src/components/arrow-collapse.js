import React from "react";

import '../styles/components/arrow-collapse.scss';

function ArrowCollapse(props) {
    return (
        <i className={props.className}></i>
    );
}

export default ArrowCollapse;